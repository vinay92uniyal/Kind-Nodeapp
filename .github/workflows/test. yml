name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    
      - name: build docker image
        run: |
             docker build -t my-node-app:latest .  /home/mnagaraju/actions-runner/Testproject-inno/
